# Demowall 项目 Bug 记录与解决方案

> 最后更新: 2026-02-21
> 项目路径: E:\AICoding\websites\demowall

## 🚨 关键问题清单

### 1. z-index 层级冲突导致下拉框被遮挡 ⚠️ 高频问题（已出现6次，已彻底解决）

**问题描述**：
- 筛选下拉框 (`.filter-dropdown`) 被页面其他元素遮挡
- 用户无法看到或点击下拉选项
- **此问题已反复出现5次**

**根本原因**：
1. 背景效果元素初始 `z-index` 设置为正数 (0, 1, 2, 3)，遮挡了页面内容
2. 内容区域 (`.hero`, `.controls`, `.projects-section`) 设置了 `z-index: 1`，创建了新的层叠上下文
3. 下拉框的 `z-index: 50` 不够高，被内容区域遮挡
4. **新增主题时未遵守 z-index 规范，导致问题第3次出现**
5. **添加 pixel 主题后问题第4次出现，触发创建防御机制**
6. **main.css 中 `.hero-content` 设置了 `z-index: 1`，导致问题第5次出现**
7. **main.css 中 `.controls` 设置了 `z-index: 10`，导致问题第6次出现**
8. **z-index-system.css 选择器过于宽泛，`.controls *` 覆盖了 `.controls` 本身，导致问题第6次持续**

**终极解决方案**：
已创建 `css/z-index-system.css` 作为 z-index 防御层，使用 `!important` 强制执行层级规范，并精确控制选择器范围。

```css
/* z-index-system.css - 最后加载，覆盖所有主题 */
.filter-dropdown {
  z-index: 100 !important;
}

.filter-group {
  z-index: 50 !important;
}

.controls {
  z-index: 50 !important;
}

.site-header {
  z-index: 100 !important;
}

/* 精确排除 .controls、.filter-groups、.filter-group */
.controls > *:not(.controls-inner),
.controls-inner > *:not(.filter-groups),
.filter-groups > *:not(.filter-group) {
  z-index: auto !important;
}
```

**最终层级结构**：
```
z-index: 9999  → .noise-overlay (顶层装饰)
z-index: 1001  → .theme-menu (主题菜单)
z-index: 1000  → .theme-switcher (主题切换器)
z-index: 100   → .site-header, .filter-dropdown (导航层) !important
z-index: auto  → .hero, .controls, .projects-section (内容层) !important
z-index: -1    → 所有背景效果 (背景层)
```

**预防措施**：
- ✅ 背景效果必须使用 `z-index: -1`
- ✅ 内容区域必须使用 `z-index: auto`
- ✅ 下拉框/弹窗必须使用 `z-index: 100+`
- ✅ **新增主题时，禁止设置任何 z-index 值**
- ✅ **z-index-system.css 必须最后加载**
- ✅ **使用 !important 强制执行规范**

**文件加载顺序**（关键）：
```html
<link rel="stylesheet" href="css/main.css">
<link rel="stylesheet" href="css/background-effects.css">
<link rel="stylesheet" href="css/theme-*.css">
<link rel="stylesheet" href="css/theme-switcher.css">
<link rel="stylesheet" href="css/z-index-system.css"> <!-- 必须最后 -->
```

---

### 2. 页面内容消失问题

**问题描述**：
- 添加背景效果后，页面所有内容不可见
- 只能看到背景动画

**根本原因**：
- 背景元素的 `z-index` 为正数，覆盖了内容层

**解决方案**：
- 参考问题 1 的解决方案
- 确保背景层 `z-index: -1`

---

## 📋 开发规范

### z-index 使用规范

| 层级范围 | 用途 | 示例 |
|---------|------|------|
| -1 | 背景装饰层 | 动态背景、粒子效果 |
| 0 或 auto | 默认内容层 | 页面主体内容 |
| 1-99 | 浮动内容 | 卡片悬停、工具提示 |
| 100-999 | 导航/下拉框 | Header、Dropdown、Modal |
| 1000+ | 全局覆盖层 | Toast、Loading、主题切换器 |

### 添加新功能检查清单

**添加背景效果时**：
- [ ] 确认 `z-index: -1`
- [ ] 确认 `pointer-events: none`
- [ ] 确认 `position: fixed`

**添加下拉框/弹窗时**：
- [ ] 确认 `z-index: 100+`
- [ ] 确认 `position: absolute` 或 `fixed`
- [ ] 测试是否被其他元素遮挡

**修改现有元素时**：
- [ ] 检查是否影响 z-index 层级
- [ ] 测试下拉框是否正常显示
- [ ] 测试背景效果是否正常

**⚠️ 添加新主题时（重要）**：
- [ ] **禁止在主题 CSS 中设置任何 z-index 值**
- [ ] 只修改颜色、字体、边框、圆角、阴影
- [ ] 依赖 `z-index-system.css` 管理层级
- [ ] 完成后测试所有主题的下拉框是否正常

---

## 🔧 调试技巧

### 快速定位 z-index 问题

```javascript
// 在浏览器控制台运行，查看所有元素的 z-index
document.querySelectorAll('*').forEach(el => {
  const zIndex = window.getComputedStyle(el).zIndex;
  if (zIndex !== 'auto') {
    console.log(el.className, 'z-index:', zIndex);
  }
});
```

### 检查层叠上下文

```javascript
// 查找创建了层叠上下文的元素
document.querySelectorAll('*').forEach(el => {
  const style = window.getComputedStyle(el);
  if (style.position !== 'static' && style.zIndex !== 'auto') {
    console.log('Stacking context:', el.className);
  }
});
```

---

## 📝 修改历史

### 2026-02-21
- **问题**: 添加动态背景后页面内容消失
  - **原因**: 背景 z-index 为正数
  - **修复**: 改为 `z-index: -1`

- **问题**: 下拉框被遮挡（第2次出现）
  - **原因**: 内容区域 `z-index: 1` 创建层叠上下文
  - **修复**: 改为 `z-index: auto`，下拉框改为 `z-index: 100`

- **问题**: 添加主题后下拉框被遮挡（第3次出现）
  - **原因**: 主题 CSS 可能覆盖 z-index 值
  - **修复**: 验证结构正确，但问题持续

- **问题**: 添加 pixel 主题后下拉框再次被遮挡（第4次出现）
  - **原因**: 缺乏强制执行机制，主题可以随意覆盖 z-index
  - **终极修复**: 创建 `css/z-index-system.css` 使用 `!important` 强制执行层级
  - **防御机制**:
    - z-index-system.css 必须最后加载
    - 所有主题禁止设置 z-index
    - 使用 !important 覆盖所有可能的冲突
  - **验证结果**: ✅ 所有5个主题测试通过

- **问题**: 添加用户系统后下拉框再次被遮挡（第5次出现）
  - **原因**: `main.css` 中 `.hero-content` 设置了 `z-index: 1`
  - **修复**: 删除 `.hero-content` 的 `z-index: 1`
  - **教训**: 需要全面检查 main.css 中所有 z-index 设置

- **问题**: 下拉框被项目卡片遮挡（第6次出现）
  - **原因1**: `main.css` 中 `.controls` 设置了 `z-index: 10`
  - **原因2**: `z-index-system.css` 选择器 `.controls *:not(.filter-dropdown)` 过于宽泛，覆盖了 `.controls` 本身的 `z-index: 50`
  - **修复**:
    1. 删除 `main.css` 中 `.controls` 的 `z-index: 10`
    2. 修改 `z-index-system.css` 选择器为精确排除：`.controls > *:not(.controls-inner)`, `.controls-inner > *:not(.filter-groups)`, `.filter-groups > *:not(.filter-group)`
  - **教训**: CSS 选择器必须精确，避免意外覆盖父元素样式

---

## 🎯 待优化项

### 性能优化
- [ ] 添加图片懒加载
- [ ] 实现虚拟滚动（大数据量）
- [ ] 提取公共数据加载模块

### 用户体验
- [ ] 改进无障碍性（ARIA 标签）
- [ ] 添加键盘导航支持
- [ ] 优化移动端触摸体验

### 代码质量
- [ ] 删除 HTML 中硬编码的筛选选项
- [ ] 统一动画时长变量
- [ ] 添加单元测试

---

## 📚 相关文件

- **主样式**: `css/main.css`
- **背景效果**: `css/background-effects.css`
- **背景脚本**: `js/background-effects.js`
- **主逻辑**: `js/app.js`
- **详情页**: `detail.html`, `css/detail.css`, `js/detail.js`

---

## 💡 经验教训

1. **z-index 必须有明确的层级规划**，不要随意设置
2. **背景效果必须使用负数 z-index**，避免遮挡内容
3. **避免不必要的层叠上下文**，优先使用 `z-index: auto`
4. **所有修改必须先测试**，确保不影响现有功能
5. **重复出现的问题需要记录**，建立知识库避免再犯
6. **高频问题需要防御机制**，使用 `!important` 强制执行规范，防止未来覆盖
